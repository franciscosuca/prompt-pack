{
  "permissions": {
    "deny": ["Agent(orchestrator-fastapi)", "Agent(playwright)", "Agent(react)", "Agent(vitest)"]
  }, // This is a temporary measure to prevent the use of certain agents until they are fully supported.
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/release-check.sh"
          }
        ]
      }
    ]
  }
}
