# prompt-pack

Repository for hosting reusable agent definitions, instruction templates, and skills that can be shared across different projects.

## Structure

- `agents/`: reusable agent definitions.
- `instructions/`: reusable instruction files and templates.
- `skills/`: reusable skill packs.

Each folder includes a local `README.md` to describe expected contents.
